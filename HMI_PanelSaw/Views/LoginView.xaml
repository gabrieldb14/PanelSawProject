<Window x:Class="HMI_PanelSaw.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Panel Saw - Login" Height="300" Width="400" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#FF9F9F9F"
        KeyDown="Window_KeyDown">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="15" Width="300">
        <StackPanel>
            <TextBlock Text="User:" FontSize="20" Foreground="#333"/>
            <TextBox x:Name="txtUsername" Height="35" Foreground="#FF333333" FontSize="20"/>
        </StackPanel>

        <StackPanel Margin="0,15,0,15">
            <TextBlock Text="Password:" FontSize="20" Foreground="#333"/>
            <PasswordBox x:Name="txtPassword" Padding="8" Height="35" Foreground="#FF333333"/>
        </StackPanel>

        <Button x:Name="BtnLogin" Content="Login" FontSize="20" Foreground="#333" Height="40" Click="BtnLogin_Click"/>
        <TextBlock x:Name="lblError" Foreground="Red" TextAlignment="Center"/>
    </StackPanel>
</Window>
